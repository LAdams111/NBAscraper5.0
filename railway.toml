[build]
buildCommand = "npm ci && npm run build"

[deploy]
# Daily cron: current season only for all players (06:00 UTC)
startCommand = "node dist/index.js --daily"
restartPolicyType = "NEVER"
cronSchedule = "0 6 * * *"
